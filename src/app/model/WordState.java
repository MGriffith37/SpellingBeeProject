package app.model;

/**
 * Created by Fraser McIntosh on 13/09/2016.
 * Enum corresponding to the state of a word during its life in a quiz and afterwards
 */
public enum WordState {
     STARTED, INCORRECT, FAULTED, FAILED, MASTERED;
}
